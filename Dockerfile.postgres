# Start with the pgvector image that includes PostgreSQL 16
FROM pgvector/pgvector:pg16

# Install PostGIS
RUN apt-get update && \
    apt-get install -y postgis postgresql-16-postgis-3 && \
    rm -rf /var/lib/apt/lists/*
